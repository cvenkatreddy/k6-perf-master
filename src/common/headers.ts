/**
 * This object defines some default headers that we need to pass to our
 * GraphQL server post request so that we don't have to re-write it
 */
export const defaultHeaders = {
  "Content-Type": "application/json",
};
